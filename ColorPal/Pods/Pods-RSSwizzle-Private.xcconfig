#include "Pods-RSSwizzle.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/RSSwizzle" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/RSSwizzle"
OTHER_LDFLAGS = -ObjC ${PODS_RSSWIZZLE_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}